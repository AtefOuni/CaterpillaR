options(prompt = "PPP> ")
options(encoding = "UTF-8")


.First <- function() {
  
  if (Sys.getenv("JAVA_HOME")!="")
    Sys.setenv(JAVA_HOME="")
  
  library(XLConnect)
  library(forecast)
  library(ggplot2)
  library(gridExtra)
    
}